create directory called build, 4.StronglyConnectedComponents/build 

To build, type "cmake .." in build directory

to compile type "make"

to run tests type "./test/tests"

to run main type "./source/SCC"