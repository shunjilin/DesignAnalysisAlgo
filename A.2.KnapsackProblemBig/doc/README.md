create directory called build, A.2.KnapsackProblemBig/build

To build, type "cmake .." in build directory

to compile type "make"

to run tests type "./test/KnapsackTest"

to run main type "./source/knapsack"