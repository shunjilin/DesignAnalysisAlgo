create directory called build, 5.Djikstra/build

To build, type "cmake .." in build directory

to compile type "make"

to run tests type "./test/MinHeapTest" and "./test/DjikstraTest"

to run main type "./source/dijkstra"